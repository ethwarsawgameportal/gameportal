import React from "react";

const PlaceholderHeader: React.FC = () => (
  <header
    className="h-14 sm:h-16 w-full border-b border-slate-200 dark:border-slate-800 sticky top-0 z-30 bg-white/60 dark:bg-slate-950/60 backdrop-blur"
    aria-label="Header placeholder"
  />
);

export default PlaceholderHeader;
